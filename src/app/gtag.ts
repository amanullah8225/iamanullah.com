// Google Analytics 4 measurement IDs. Events are sent to every property in
// this list (see app/layout.tsx for the loader + app/analytics.tsx for
// client-side route-change tracking).
export const GA_MEASUREMENT_IDS = ['G-9ZNE7235KL', 'G-W9304M5RCW'];
